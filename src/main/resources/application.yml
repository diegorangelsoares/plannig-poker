server:
  port: ${PORTA:8082}

spring:
  application:
    name: planning-poker-service

  jpa:
    hibernate:
      ddl-auto: update
    database: POSTGRESQL

  datasource:
#    tomcat:
#      default-auto-commit: false
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:planning}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
    driverClassName: org.postgresql.Driver
#    type: com.zaxxer.hikari.HikariDataSource
#    hikari:
#      minimum-idle: 5
#      maximum-pool-size: 20
#      pool-name: pool-testes
#      auto-commit: false
#  #  liquibase:
##    enabled: true
##    change-log: classpath:/data/db.changelog.yaml
#  jackson:
#    time-zone: Brazil/East
#    locale: pt-BR
#    serialization:
#      write-dates-as-timestamps: false
#  sql:
#    init:
#      continue-on-error: true
#      platform: postgres

application:
  api-info:
    titulo: Planning-poker Service API
    descricao: Api para controle de Planning
    versao: '@project.version@'
    pacote: com.diego.testes
